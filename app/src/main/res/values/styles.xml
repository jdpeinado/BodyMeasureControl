<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:windowFullscreen">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="AppTheme.Normal" parent="AppTheme">
        <item name="android:textSize">@dimen/normalMeasurementDetail</item>
    </style>
    <style name="AppTheme.High" parent="AppTheme">
        <item name="android:textSize">@dimen/highMeasurementDetail</item>
        <item name="android:textStyle">bold</item>
        <item name="android:color">@android:color/black</item>
    </style>
    <style name="AppTheme.Small" parent="AppTheme">
        <item name="android:textSize">@dimen/smallMeasurementDetail</item>
    </style>

    <style name="FulllScreenDialogStyle" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowEnterAnimation">@android:anim/slide_in_left</item>
        <item name="android:windowExitAnimation">@android:anim/slide_out_right</item>
    </style>
    
</resources>
